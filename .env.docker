# ===========================================
# Configuración Docker - Copiar a .env
# ===========================================

# Puerto expuesto (default: 3003)
PORT=3003

# API Keys (REQUERIDAS)
OPENAI_API_KEY=your_openai_api_key_here

# API Keys opcionales
ANTHROPIC_API_KEY=
GOOGLE_AI_API_KEY=

# JWT Secret para autenticación (cambiar en producción)
JWT_SECRET=change-this-to-a-secure-random-string-in-production

# URL de la API para el frontend (dejar vacío si es mismo dominio)
# Si usas un dominio: https://tudominio.com
# Si usas IP: http://tu-ip:3003
VITE_API_BASE_URL=

# Dominio para Traefik (solo si usas docker-compose.prod.yml)
DOMAIN=mediciones.tudominio.com
